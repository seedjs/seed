#!/usr/bin/env seed

// TODO: Find nearest package and automatically register it so you can work
// inside of a project

var repl = require('default:repl');
repl.start('seed> ');
repl.scope.require = require;
repl.scope.module  = module;
